[project]
name = "mcp-interfaces"
version = "0.1.0"
description = "Tagentacle MCP Interface Definitions — JSON Schema for /mcp/directory Topic"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
